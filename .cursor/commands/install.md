# Install

Build Release and install to `/Applications/Sidekick.app` via `make install`.

Then run `scripts/smoke.sh` unless the user asked only for the copy. Report `doctor --json` if the app does not answer.
