# Verify

Follow the `verify-module` skill and `AGENTS.md` section «Проверка».

Run `make verify`. If the change touches install, the control socket, CLI or feature lifecycle, also run `make install` and `scripts/smoke.sh`.
